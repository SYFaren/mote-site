mote / mote-x — tiny GUI text editors by SYFaren.

This is a long line that should wrap when soft-wrap is enabled so the gallery shot shows wrapping clearly across the viewport without horizontal scrolling forever and ever.

Keys worth knowing: Ctrl+F find, Ctrl+G goto line, Ctrl+T theme, Ctrl+W wrap.
